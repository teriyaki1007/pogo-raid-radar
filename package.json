{
  "name": "pogo-raid-radar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build && cp landing/index.html dist/index.html && cp -a apps/lantern-courts dist/lantern-courts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "description": "Raid Radar — Pokémon GO raid schedule static site"
}
