{
  "name": "pogo-raid-radar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build && cp landing/index.html dist/index.html && cp -a apps/lantern-courts dist/lantern-courts && cp -a apps/ap-csa dist/ap-csa && cp -a apps/homework dist/homework && cp -a apps/beyblade dist/beyblade && cp -a apps/recipes dist/recipes && cp -a apps/scores dist/scores",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "description": "Raid Radar — Pokémon GO raid schedule static site"
}
